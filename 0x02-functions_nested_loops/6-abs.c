#include "main.h"
/**
 * _abs - computes absolute value of an integer
 *
 */
int _abs(int a)
{
	int a;

	if (a >= 0)
	{
		return (_abs(a));
	}
	else if (a < 0)
	{
		return (_abs(a));
	}
	return (0);
}
